// C:\DataFlex\FutbolWebApp\AppSrc\SelectEquipo.wo
// Equipo
//

Use cWebView.pkg
Use cWebList.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebColumnButton.pkg
Use cWebColumn.pkg
Use ZoomEquipo.wo

Use cligaDataDictionary.dd
Use cequipoDataDictionary.dd

Object oSelectEquipo is a cWebView
    Set psCaption to "Equipo"
    Set peWebViewStyle to wvsDrilldown
    Set peViewType to vtSelect
    Set piColumnCount to 12
    Set pbShowCaption to False
    Set piMaxWidth to 1024
    
    Property String psFiltroLigaID

    Object oliga_DD is a cligaDataDictionary
    End_Object 

    Object oequipo_DD is a cequipoDataDictionary
        Set DDO_Server to oliga_DD
        
        Procedure OnConstrain
            String sLigaID
        
            Forward Send OnConstrain
        
            Get psFiltroLigaID of oSelectEquipo to sLigaID
        
            If (sLigaID <> "") Begin
                Constrain equipo.LigaID eq sLigaID
            End
        End_Procedure
    End_Object 

    Set Main_DD to oequipo_DD
    Set Server  to oequipo_DD

    Object oList is a cWebList
        Set piColumnSpan to 0
        Set psCSSClass to "MobileList"
        Set pbServerOnRowClick to True
        Set pbFillHeight to True
        Set pbShowHeader to True
        Set piSortColumn to 0

        Object oequipoNombre is a cWebColumn
            Entry_Item equipo.Nombre
            Set psCaption to "Nombre"
            Set piWidth to 1491
        End_Object 

        Object oDetailButton is a cWebColumnButton
            Set psCaption to "btn"
            Set piWidth to 45
            Set pbFixedWidth to True
            Set pbResizable to False
            Set piListRowSpan to 1
            Set psBtnCssClass to "WebButtonIcon WebIcon_Info"
            Set peAlign to alignRight

            WebRegisterPath ntNavigateForward oZoomEquipo

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                String sNombreEquipo
            
                Move True to NavigateData.bReadOnly
            
                Get Field_Current_Value of oequipo_DD Field equipo.Nombre to sNombreEquipo
            
                If (sNombreEquipo <> "") Begin
                    Set psCaption of oZoomEquipo to sNombreEquipo
                End
            End_Procedure
        End_Object 

        WebRegisterPath ntNavigateForward oZoomEquipo

        Procedure OnRowClick String sRowId
            tWebNavigateData NavigateData

            Get GetNavigateData to NavigateData

            Case Begin
                Case (NavigateData.eNavigateType = nfFromParent)
                    Send NavigateForward of oZoomEquipo Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromChild)
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromMain)
                    Send NavigateClose Self
                    Case Break

                Case Else
                    Send NavigateForward of oZoomEquipo Self
            Case End
        End_Procedure

        Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
            String sNombreEquipo
            
            Move True to NavigateData.bReadOnly
            
            Get Field_Current_Value of oequipo_DD Field equipo.Nombre to sNombreEquipo
            
            If (sNombreEquipo <> "") Begin
                Set psCaption of oZoomEquipo to sNombreEquipo
            End
        End_Procedure

    End_Object 

    Object oActionGroup is a cWebMenuGroup

        Object oSearch is a cWebMenuItem
            Set psCSSClass to "WebPromptMenuItem"
            Set psCaption to "Search"

            Procedure OnClick
                Send Search of oList
            End_Procedure
        End_Object 

        Object oNewButton is a cWebMenuItem
            Set psCSSClass to "WebClearMenuItem"
            Set psCaption to "New"

            WebRegisterPath ntNavigateForward oZoomEquipo

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bNewRecord
                Set psCaption of oZoomEquipo to "Nuevo Equipo"
            End_Procedure
        End_Object 

    End_Object 

    Procedure OnNavigateForward tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        Forward Send OnNavigateForward NavigateData hoInvokingView hoInvokingObject

        WebSet pbRender of oNewButton to True
        WebSet pbRender of oDetailButton to True
        
        WebSet psCaption to "Equipos"

        Send Rebuild_Constraints of oequipo_DD

        Case Begin
            Case (NavigateData.eNavigateType = nfFromParent)
                WebSet pbRender of oDetailButton to False
                Case Break

            Case (NavigateData.eNavigateType = nfFromChild)
                Case Break

            Case (NavigateData.eNavigateType = nfFromMain)
                WebSet pbRender of oDetailButton to False
                Case Break

            Case Else
                WebSet pbRender of oDetailButton to False
        Case End
    End_Procedure
    
    Procedure OnNavigateBack tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        Forward Send OnNavigateBack NavigateData hoInvokingView hoInvokingObject
    
        Set psFiltroLigaID to ""
        Send Rebuild_Constraints of oequipo_DD
    End_Procedure

End_Object